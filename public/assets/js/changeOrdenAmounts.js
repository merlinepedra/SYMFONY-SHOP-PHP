//import '../styles/changeOrdenAmounts.css'

/*
var formulario = $('#miFormulario').data('form')
$('.minus').click(function(){
    var cantidadItem = $(this).closest('.cantidad')
    var newAmount = parseInt(cantidadItem.innerHtml) - 1

    if(newAmount > 0)
    {
        formulario.val(cantidadItem.data('orden'), newAmount)
        cantidadItem.innerHtml = String(newAmount)
    }
})

$('.plus').click(function(){
    var cantidadItem = $(this).closest('.cantidad')
    var newAmount = parseInt(cantidadItem.innerHtml) + 1

    formulario.val(cantidadItem.data('orden'), newAmount)
    cantidadItem.innerHtml = String(newAmount)
})
*/
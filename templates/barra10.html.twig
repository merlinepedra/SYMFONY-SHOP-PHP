{% block stylesheets %}
    <link rel="stylesheet" href="{{asset('assets/styles/barra10.css')}}">
{% endblock %}

{% block javascripts %}
    <script src="{{asset('assets/js/barra10.js')}}" defer></script>
{% endblock %}

{% set current_url = path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')) | split('/') %}
{% set url_length = current_url|length %}
                    
                    
{% if current_url|last == 'index' %}
    <div>
        <div class="path estás_en">
            <span style="color: rgb( 255, 255, 255 );">Estás en: </span>
            <span>Inicio</span>
        </div>
        <div class="row" id="visualizacion_listado" style="padding-left: 10px; margin-top: 10px; margin-bottom: 20px; padding-bottom: 0px; border-bottom: 1px solid rgb( 224, 224, 224 );">
            <div class="col-md-4"></div>
            <ul class="col-md-8" style="list-style: none;">
                    <li>
                        Mostrar: 

                        <div class="dropdown" style="display: inline;">
                            <a class="dropdown-toggle" role="button" data-toggle="dropdown">
                                <span class='selectorValue'>10</span>
                                <i class='fa fa-caret-down sidebar_carets'></i>
                            </a>
                            <ul class="dropdown-menu">
                              <li><a href="#">10</a></li>
                              <li><a href="#">20</a></li>
                              <li><a href="#">30</a></li>
                            </ul>
                        </div>
                        
                        <span style="border-right: 2px solid #b6b1b1; padding-right: 15px; margin-right: 15px;"></span>
                    </li>
                    <li>
                        <div class="dropdown" style="display: inline;">
                            <a class="dropdown-toggle" role="button" data-toggle="dropdown">
                                <span class='selectorValue'>Baratos arriba</span>
                                <i class='fa fa-caret-down sidebar_carets'></i>
                            </a>
                            <ul class="dropdown-menu">
                              <li><a href="#">Baratos arriba</a></li>
                              <li><a href="#">Caros arriba</a></li>
                            </ul>
                        </div>
                        
                        <span style="border-right: 2px solid #b6b1b1; padding-right: 15px; margin-right: 15px;"></span>
                    </li>
                    <li style="padding-right: 30px;">
                        <img src="{{ asset('exact_images/Icono_Lista.png') }}">
                        <img src="{{ asset('exact_images/Icono_Rejilla.png') }}">
                    </li>
            </ul>
        </div>
    </div>
{% endif %}

{% if current_url|slice(0, url_length -1)|last == 'productoView' %}
    <div class="path estás_en">
        <span style="color: rgb( 255, 255, 255 );">Estás en: </span>
        <span>Inicio</span>
        
        <i class="fa fa-caret-right"></i>
        {% for x in products_repo.getPath(producto.id) %}
                <span>{{x}}</span>
                <i class="fa fa-caret-right"></i>
        {% endfor %}
    </div>
{% endif %}
{% extends 'index/base.html.twig' %}

{% block title %}Home{% endblock %}

{% block stylesheets %}
        {{parent()}}
        {{encore_entry_link_tags('viewProduct')}}
{% endblock %}

{% block body2 %}
{% if producto %}
<div class="col-sm-8">
                <h2>{{ producto.nombre }}<h6 class="badge badge-pill" style="color: white; background-color:  orangered; box-shadow: 3px 5px 4px #888888;">{{ producto.categoria.nombre }}</h6></h2>
        <br>
        <p>{{ producto.descripcion }}</p>
        <br>
        Creado: <strong>{{ producto.Created|format_datetime('medium', 'short', '', 'UTC') }}</strong>
        <br>
        Precio: <strong>{{ producto.preciounidad }}</strong>$
        {#
        #}

        <figure> 
                {% for foto in producto.fotos %}
                        {#<img width="300" height="300" alt="picture">#}
                        <div class="box" style="background-color: {{ foto }};"></div>
                {% endfor %}
        {#<img width="300" height="300" alt="picture" src="{{ asset('./images/1.jpg') }}">#}
        </figure>
        
        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                <a href="{{url('carrito_plus', {'productoid': producto.id})}}">Añadir al carrito</a>
        {% else %}
                Debes loguearte para realizar compras.
        {% endif %}

</div>
{% else %}
        <h2>Ese producto no existe.</h2>
{% endif %}
{% endblock %}